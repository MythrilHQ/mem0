# Required. Pin an exact, tested, non-prerelease Traefik v3 tag (for example,
# the current v3.x.y patch approved for this deployment). Do not use `latest`.
TRAEFIK_IMAGE_TAG=

# Required for Let's Encrypt certificate registration.
ACME_EMAIL=

# Optional.
TRAEFIK_LOG_LEVEL=INFO
